# Grounded — runtime dependencies
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
tenacity>=8.3
pypdf>=4.2
langchain-text-splitters>=0.3
langchain-google-genai>=2.0
chromadb>=0.5
rank-bm25>=0.2.2
langgraph>=0.2
fastapi>=0.110
uvicorn[standard]>=0.29
fastmcp>=2.0

# dev / test
pytest>=8.0
httpx>=0.27

# optional (not required to run the core)
# sentence-transformers>=3.0                     # cross-encoder reranker (pulls torch)
# ragas>=0.2                                     # extra RAG eval metrics
# langfuse>=2.0                                  # tracing dashboard
# transformers, peft, trl, datasets, accelerate # Week 8 LoRA fine-tuning (Colab GPU)
